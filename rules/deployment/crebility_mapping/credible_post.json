{
  "name": "credible_post",
  "description": "checks the post and flags as credible",
  "condition": "post.veracity_true > 0.8 && post.stance_comment > 0.8 && post.stance_support > 0.8 && post.stance_deny < 0.2 && post.misinfo_credibility_value > 0.66 && post.misinfo_credibility_confidence > 0.5",
  "actions": [
    " post.final_credibility = \"credible\";"
  ]
}
