# Rules-production
rulePath=rules/coinform.json

# Threshold of each modules
threshold.similarity= 0.5
threshold.semantic = 0.5
threshold.stance = 0.5
threshold.veracity = 0.5
threshold.credibility = 0.5
