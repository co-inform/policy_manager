[{
  "name": "uncertain_credible_inject",
  "description": "the system acknowledges the user for posting uncertain tweets (explainability) and tiggers inject",
  "condition": "callback.getFinalCredibility() == Credibility.credible_uncertain",
  "actions": [
    "callback.inject();"
  ]
},
{
  "name": "uncertain_credible_inoculate",
  "description": "the system acknowledges the user for posting uncertain credible tweets (explainability) and tiggers inoculate",
  "condition": "callback.getFinalCredibility() == Credibility.credible_uncertain",
  "actions": [
    "callback.inoculate();"
  ]
}
]
