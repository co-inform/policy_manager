[
  {
    "name": "uncertain credibility",
    "description": "send not credible message",
    "priority": 11,
    "condition": "callback.getFinalCredibility() == Credibility.credible_uncertain",
    "actions": [
      "callback.read_msg('templates/uncertain_credibility_msg')",
      "callback.blur()"

    ]
  },
  [
    {
      "name": "uncertain credibility",
      "description": "send uncertain credibility message",
      "priority": 12,
      "condition": "callback.getFinalCredibility() == Credibility.credible_uncertain",
      "actions": [
        "callback.read_msg('templates/uncertain_credibility_msg')",
        "callback.await()"

      ]
    }
  ]

]
