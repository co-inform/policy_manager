[{
  "name": "mostly_not_credible_notify",
  "description": "the system acknowledges the user for posting credible tweets (explainability) and tiggers notify",
  "condition": "\"mostly not credible\".equals(final_credibility) && \"notify\".credible_preference",
  "actions": [
    "callback.notify();"
  ]
},
{
  "name": "mostly_not_credible_alternative",
  "description": "the system acknowledges the user for posting credible tweets (explainability) and tiggers notify",
  "condition": "\"mostly not credible\".equals(final_credibility) && \"alternative\".credible_preference",
  "actions": [
    "callback.alternative();"
  ]
}
]
